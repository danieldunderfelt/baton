Baton fixes, 22 September 2026

Scope: implement the release review's runtime, discovery, CLI, instruction, maintenance and sharing-service fixes. Release packaging, signing automation, publishing, licensing and deployed service setup remain deferred at the user's request.

Done means valid adapters register without approval or mandatory testing; repeated registration and blocks remain stable; session, failover, streaming and persistence reproductions have regression coverage; catalogs use account environments without blocking execution; ordinary CLI operations need no redundant confirmation; cancellation works across processes; ratings export is on demand; sharing endpoints enforce fair limits. Root and site checks must pass, with any live-only limits stated explicitly.

Work units:

- [x] Remove adapter approval, simplify stored state and make the schema authoritative.
- [x] Fix session ownership, attempt metadata, output errors, persistence failure and retention; support cross-process cancellation and explicit resume options.
- [x] Make discovery asynchronous and account-aware, with accurate permission capabilities and shared diagnostic policy.
- [x] Simplify CLI help/import/output, update installed skills and make ratings export on demand.
- [x] Shorten installed instructions and update product documentation/privacy wording.
- [x] Add sharing-service rate/storage limits and pagination.
- [x] Integrate changes, run targeted regressions and complete root/site checks.

Independent implementations use isolated worktrees. Integration owns cross-module interface changes and verifies the actual files. Baseline evidence is in release-readiness-2026-09-22.md and review-2026-09-22/. Decisions and verified checkpoints are recorded in fix-decisions-2026-09-22.tsv.

Completed verification:

- `bun run check`: passed.
- `env -u BATON_LIVE_TESTS bun run test`: 800 passed, 12 live tests skipped, zero failures across 32 files.
- `bun run build`: passed. The generated local executable was ad-hoc signed for macOS testing; `bun run check:release` passed against that executable. The smoke checker now expects command-specific help.
- Site `bun run test`: 22 passed. Astro check: zero errors and warnings; 17 hints concern Bun test matcher await types. Site build and `check:production` passed against the local built Worker.
- Integration regressions cover late configuration changes during asynchronous discovery, anonymous duel cancellation, concurrent installation tracking, and aggregate diagnostics with disabled registrations.

Independent native reviewers reproduced and checked the integration issues. Baton MCP and a different-family review were unavailable; no agent CLI was invoked as a substitute. Delegation regressions use fake executables and real SQLite. Sharing limits use real local D1/Miniflare, including concurrent writes.

Release work remains deferred: signing/build automation, CI/platform compatibility, repository release documents, production deployment and a real OAuth/share/revoke check. No live model turns or full live resume matrix were run. Historical skill installations outside known home/current-project locations need one reinstall to enter tracking. Run retention is bounded by count and preserves active work; it has no separate byte quota.

The historical reproduction script targets baseline commit `730c61d`; current executable regressions live beside the implementation in `src/` and `test/`. The earlier release-readiness report is preserved as the pre-fix record.
