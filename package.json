{
  "name": "baton",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "baton": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build --compile --outfile dist/baton src/index.ts",
    "build:all": "bun build --compile --target=bun-darwin-arm64 --outfile dist/baton-darwin-arm64 src/index.ts && bun build --compile --target=bun-darwin-x64 --outfile dist/baton-darwin-x64 src/index.ts && bun build --compile --target=bun-linux-x64 --outfile dist/baton-linux-x64 src/index.ts && bun build --compile --target=bun-linux-arm64 --outfile dist/baton-linux-arm64 src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/core": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
